# Pinned MasterDnsVPN (Go server) version embedded in the coffeeblack-vpn binary.
# build.rs reads this file to validate that the vendored blob matches the
# expected version, and prints the version into the binary at compile time.
#
# x86_64 only — same target story as the rest of the vendor tree
# (see vendor/README.md for the rationale).
#
# Upstream: https://github.com/masterking32/MasterDnsVPN
# Release asset: MasterDnsVPN_Server_Linux_AMD64.tar.gz

MDNSVPN_VERSION = v2026.06.13.234407-7de2476

# SHA-256 of the *stripped* MasterDnsVPN server ELF. The runtime extractor
# uses this to skip re-extraction when the on-disk file already matches.
# (Distinct from the upstream tarball SHA `3c6233ec…` recorded in vendor/README.md
# under the MDNSVPN provenance section.)
#
# Source: stripped output of the Go ELF inside MasterDnsVPN_Server_Linux_AMD64.tar.gz.
# vendor/update.sh strips debug info before gzipping to keep the bundled blob
# under ~2 MB instead of the 6.6 MB unstripped form.

MDNSVPN_AMD64_SHA256 = aebb7eb879c742135327b147f66e267e1587c47c9043de9a41811fe3eec8c126
