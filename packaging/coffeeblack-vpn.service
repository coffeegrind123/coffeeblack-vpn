[Unit]
Description=coffeeblack-vpn — AmneziaWG VPN + proxy manager with web UI
Documentation=https://github.com/coffeegrind123/coffeeblack-vpn
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
Restart=on-failure
RestartSec=5s

# --- Run as root ------------------------------------------------------------
# coffeeblack-vpn owns the AmneziaWG interface lifecycle itself: on startup it runs
# `awg-quick up cb0` (writing /etc/coffeeblack/conf/cb0.conf), and on shutdown it
# tears the interface down. That requires:
#   * CAP_NET_ADMIN  — create/configure the cb0 interface, program routes and
#                      the nftables/iptables firewall.
#   * CAP_SYS_MODULE — load the amneziawg kernel module on demand.
# The simplest correct model (and the one upstream's Docker image uses) is to
# run as root. The ambient capabilities below are declared explicitly so the
# intent is documented and so a future move to a non-root User= keeps working.
#
# --- Optional: privilege separation ------------------------------------------
# With `coffeeblack-vpn-helper.service` running, this process needs NO capabilities
# at all: every `awg-quick` / `awg` / `nft` call and the interface config write
# go over the helper's Unix socket instead. To switch:
#
#   1. systemctl enable --now coffeeblack-vpn-helper.service
#   2. useradd --system --no-create-home --shell /usr/sbin/nologin coffeeblack
#   3. set COFFEEBLACK_HELPER_GID in the helper unit to that user's gid
#   4. uncomment the SIX lines below and comment out User=root/Group=root,
#      both capability lines, and the NoNewPrivileges line further down
#   5. systemctl daemon-reload && systemctl restart coffeeblack-vpn
#
# The capability and NoNewPrivileges parts of step 4 are not optional polish.
# The root-mode bounding set below carries CAP_SETUID, CAP_SETGID,
# CAP_SYS_ADMIN and CAP_DAC_OVERRIDE, and privilege gain is permitted. Switch
# User= while leaving either as-is and the web process can climb back to root,
# which is exactly what the helper exists to prevent. The helper unit already
# sets a minimal bounding set and forbids privilege gain; this one must match.
#
# Left opt-in rather than default because it needs an account and a second
# unit; an upgrade should not silently change how the service is deployed.
# Note the bundled transports that bind low ports (Xray on 443, MasterDnsVPN
# on 53) still need CAP_NET_BIND_SERVICE — add it back if you enable those.
#
#User=coffeeblack
#Group=coffeeblack
#Environment=COFFEEBLACK_HELPER_SOCKET=/run/coffeeblack/helper.sock
#AmbientCapabilities=
#CapabilityBoundingSet=
#NoNewPrivileges=yes
#
User=root
Group=root
AmbientCapabilities=CAP_NET_ADMIN CAP_SYS_MODULE
CapabilityBoundingSet=CAP_NET_ADMIN CAP_SYS_MODULE CAP_NET_RAW CAP_NET_BIND_SERVICE CAP_DAC_OVERRIDE CAP_CHOWN CAP_FOWNER CAP_SETUID CAP_SETGID CAP_SYS_ADMIN

# --- Binary and configuration ------------------------------------------------
ExecStart=/usr/local/bin/coffeeblack-vpn

# --- Secret encryption key (optional but recommended) ------------------------
# Encrypts secrets at rest (TOTP seeds) so a stolen database or backup yields
# nothing usable. Create the machine-bound credential once:
#
#   openssl rand -base64 32 \
#     | systemd-ask-password --no-tty -n \
#     | systemd-creds encrypt --name=SECRET_KEY - /etc/coffeeblack/secret.cred
#
# or more simply:
#
#   openssl rand -base64 32 > /tmp/k && \
#     systemd-creds encrypt --name=SECRET_KEY /tmp/k \
#       /etc/coffeeblack/secret.cred && shred -u /tmp/k
#
# then uncomment both lines. systemd decrypts it into a tmpfs at start, so the
# plaintext key never touches disk. Without it the service still runs and logs
# a warning that secrets are stored unencrypted.
#
# NOTE: this protects the database at rest. It does not protect a live
# compromise — the running process necessarily holds the key.
#LoadCredentialEncrypted=SECRET_KEY:/etc/coffeeblack/secret.cred
#Environment=COFFEEBLACK_SECRET_KEY_PATH=%d/SECRET_KEY

# EnvironmentFile is optional (leading '-'): the binary has working defaults and
# the setup wizard can seed the first admin if the file is absent.
EnvironmentFile=-/etc/coffeeblack/coffeeblack.env

# Best-effort module load before the binary starts. The leading '-' means a
# failure here (e.g. module built into the kernel, or not yet built after a
# kernel upgrade) does NOT block startup: the binary retries via CAP_SYS_MODULE
# and, failing that, still serves the web UI so the operator can recover.
ExecStartPre=-/sbin/modprobe amneziawg

# Give awg-quick's teardown time to run cleanly on stop/restart.
TimeoutStopSec=30s
KillMode=mixed

# File-descriptor ceiling. The DPI proxy uses one backend UDP socket per
# active session, so the fd count tracks the admin "Max sessions" setting
# (default 2048). 65536 covers the default comfortably; if you raise Max
# sessions toward its 65536 cap, raise this to match (e.g. 131072).
LimitNOFILE=65536

# --- Hardening ---------------------------------------------------------------
# Kept deliberately moderate. coffeeblack-vpn shells out to `awg-quick`, which in
# turn runs `ip`, `iptables`/`nft`, and `sysctl`, and writes generated configs
# under /etc/coffeeblack/conf. Over-tight sandboxing breaks those, so the directives
# below are the strongest set that still lets the interface come up.
#
# Do NOT add any of the following — each one would break a required operation:
#   * ProtectKernelModules=yes  → blocks modprobe amneziawg (needs CAP_SYS_MODULE)
#   * ProtectKernelTunables=yes → blocks sysctl net.ipv4.ip_forward writes
#   * PrivateNetwork=yes        → hides the interface/namespace entirely
#   * RestrictAddressFamilies=  → blocks AF_NETLINK used to program the interface
#
# Root mode only. In privilege-separated mode this MUST become `yes` (step 4
# above): the unprivileged web process needs no privilege gain, and leaving it
# `false` alongside a bounding set carrying CAP_SETUID/CAP_SETGID/CAP_SYS_ADMIN
# hands root back to any RCE in the HTTP layer — defeating the split entirely.
NoNewPrivileges=false
# No core dumps. A dump of this process contains the decrypted secret key,
# every session token, and any peer key material in flight — written to disk
# by the kernel, outside every other protection here. Costs nothing to refuse.
LimitCORE=0
ProtectSystem=full
ProtectHome=yes
PrivateTmp=yes
ProtectControlGroups=yes
RestrictSUIDSGID=yes
RestrictRealtime=yes

# ProtectSystem=full makes /etc read-only; carve out the paths the service must
# write. COFFEEBLACK_CONF_DIR (default /etc/coffeeblack/conf) holds cb0.conf, the SQLite
# DB (durable, IN_MEMORY=false on bare metal), and the extracted subprocess
# working dirs (xray/, mtproxy/, dns/, mdnsvpn/).
ReadWritePaths=/etc/coffeeblack/conf
ReadWritePaths=-/etc/coffeeblack
ReadWritePaths=-/var/lib/coffeeblack
ReadWritePaths=-/proc/sys/net

# --- Environment defaults ----------------------------------------------------
# Bare-metal durable install: keep the roster on disk rather than in RAM.
# Anything set in EnvironmentFile overrides these.
Environment=IN_MEMORY=false
Environment=COFFEEBLACK_CONF_DIR=/etc/coffeeblack/conf
Environment=COFFEEBLACK_DB_PATH=/etc/coffeeblack/conf/coffeeblack.db
Environment=PORT=51821
Environment=HOST=0.0.0.0

[Install]
WantedBy=multi-user.target
